# 姿势厅功能实现说明

## 📋 功能概述

姿势厅是一个展示各类拍照姿势的页面，用户可以浏览、搜索和使用不同分类的姿势。

## ✅ 已实现功能

### 1. 主页面 (pages/gallery/index)

#### 功能特性：
- ✅ **搜索框**：支持搜索姿势（暂未连接后端接口）
- ✅ **分类标签**：10个分类标签，可点击切换（浪漫满屋、不露脸、情侣、闺蜜、自拍、鲜花、古灵精怪、创意照、超市、合照）
- ✅ **姿势展示**：
  - 本地 Pose
  - 美少女
  - 自拍
  - 情侣
- ✅ **横向滑动**：每个分类可以横向滑动查看更多姿势
- ✅ **每行两张图**：姿势卡片宽度设置为 320rpx，间距 16rpx
- ✅ **"全部 >" 按钮**：点击跳转到分类详情页
- ✅ **底部广告栏**：固定显示优惠信息

#### 登录权限控制：
- ✅ **未登录可访问**：所有浏览功能均可使用
- ✅ **"使用"按钮受限**：点击时检查登录状态，未登录弹出登录提示

### 2. 分类详情页 (pages/category-detail/category-detail)

#### 功能特性：
- ✅ **网格布局**：每行显示2张姿势图片
- ✅ **自定义导航栏**：显示分类名称和关闭按钮
- ✅ **滚动加载**：支持滚动查看所有姿势
- ✅ **点击详情**：点击姿势卡片跳转到详情页
- ✅ **底部广告栏**：与主页面保持一致

### 3. 姿势详情页 (pages/pose-detail/pose-detail)

#### 功能特性：
- ✅ **大图展示**：全屏展示姿势图片
- ✅ **黑色背景**：突出图片显示效果
- ✅ **自定义导航栏**：右上角显示关闭按钮
- ✅ **姿势信息**：显示姿势名称和描述
- ✅ **底部操作按钮**："使用这个姿势"按钮

### 4. 数据层 (data/poses.js)

#### 数据结构：
```javascript
{
  id: 'pose_id',           // 姿势ID
  name: '姿势名称',         // 姿势名称
  image: '/path/to/image', // 姿势图片路径
  category: 'category',    // 所属分类
  description: '描述'      // 姿势描述（可选）
}
```

#### 提供的方法：
- `getCategories()` - 获取分类列表
- `getPosesByCategory(categoryKey, limit)` - 获取指定分类的姿势（可限制数量）
- `getAllPosesByCategory(categoryKey)` - 获取指定分类的所有姿势
- `searchPoses(keyword)` - 搜索姿势
- `getPoseById(poseId)` - 根据ID获取姿势详情

## 🔧 待接入后端接口

所有数据目前使用模拟数据，需要替换为实际接口：

### 1. 姿势列表接口
```javascript
// 在 miniprogram/pages/gallery/index.js 的 loadPoses() 方法中
// TODO: 替换为实际接口调用
wx.request({
  url: 'YOUR_API_URL/poses/list',
  data: {
    category: category,
    limit: 6
  },
  success: (res) => {
    this.setData({
      localPoses: res.data.local,
      beautyPoses: res.data.beauty,
      selfiePoses: res.data.selfie,
      couplePoses: res.data.couple
    })
  }
})
```

### 2. 搜索接口
```javascript
// 在 miniprogram/pages/gallery/index.js 的 onSearch() 方法中
// TODO: 调用搜索接口
wx.request({
  url: 'YOUR_API_URL/poses/search',
  data: {
    keyword: keyword
  },
  success: (res) => {
    // 处理搜索结果
  }
})
```

### 3. 分类详情接口
```javascript
// 在 miniprogram/pages/category-detail/category-detail.js 的 loadPoses() 方法中
// TODO: 替换为实际接口调用
wx.request({
  url: 'YOUR_API_URL/poses/category',
  data: {
    category: this.data.category
  },
  success: (res) => {
    this.setData({ poses: res.data })
  }
})
```

### 4. 姿势详情接口
```javascript
// 在 miniprogram/pages/pose-detail/pose-detail.js 的 loadPoseDetail() 方法中
// TODO: 替换为实际接口调用
wx.request({
  url: 'YOUR_API_URL/poses/detail',
  data: {
    id: this.data.poseId
  },
  success: (res) => {
    this.setData({ pose: res.data })
  }
})
```

## 🎨 样式说明

### 设计规范：
- **主色调**：白色背景 + 灰色分区
- **强调色**：黄橙渐变（#FFB800 -> #FFA000）用于"使用"按钮
- **蓝色渐变**（#4A90E2 -> #357ABD）用于广告栏
- **卡片尺寸**：320rpx × 420rpx
- **圆角**：16rpx（卡片）、48rpx（按钮）
- **间距**：16rpx（卡片间距）、24rpx（页面边距）

## 📱 页面配置

### app.json 已注册页面：
```json
{
  "pages": [
    "pages/gallery/index",           // 姿势厅主页
    "pages/category-detail/category-detail",  // 分类详情页
    "pages/pose-detail/pose-detail"  // 姿势详情页
  ]
}
```

## 🔗 页面跳转逻辑

```
姿势厅主页 (gallery/index)
    │
    ├─> 点击"全部 >" ──> 分类详情页 (category-detail)
    │                        │
    │                        └─> 点击姿势卡片 ──> 姿势详情页 (pose-detail)
    │
    ├─> 点击姿势卡片 ──> 姿势详情页 (pose-detail)
    │
    └─> 点击"使用"按钮 ──> 检查登录 ──> 未登录提示 / 跳转拍摄页面（待实现）
```

## 📝 注意事项

1. **图片资源**：
   - 搜索图标：`/images/icons/question.svg`
   - 关闭图标：需要添加 `/images/icons/close.png`
   - 占位图片：当前使用项目中已有的示例图片

2. **登录判断**：
   - 使用 `authUtil.isLoggedIn()` 检查登录状态
   - 未登录时点击"使用"按钮会弹出登录提示

3. **后续开发**：
   - 与拍摄功能连通
   - 接入真实后端接口
   - 完善搜索功能
   - 标签筛选功能实现

## 🚀 测试建议

1. **浏览测试**：
   - 检查各分类的姿势是否正常显示
   - 测试横向滑动是否流畅
   - 验证"全部 >"跳转是否正确

2. **交互测试**：
   - 点击姿势卡片查看详情
   - 测试标签切换效果
   - 验证搜索框输入

3. **权限测试**：
   - 未登录状态点击"使用"按钮
   - 登录后点击"使用"按钮的行为

## 📦 文件清单

- ✅ `miniprogram/data/poses.js` - 姿势数据配置
- ✅ `miniprogram/pages/gallery/index.wxml` - 姿势厅页面结构
- ✅ `miniprogram/pages/gallery/index.wxss` - 姿势厅样式
- ✅ `miniprogram/pages/gallery/index.js` - 姿势厅逻辑
- ✅ `miniprogram/pages/gallery/index.json` - 姿势厅配置
- ✅ `miniprogram/pages/category-detail/category-detail.*` - 分类详情页（4个文件）
- ✅ `miniprogram/pages/pose-detail/pose-detail.*` - 姿势详情页（4个文件）

---

**实现状态**：✅ 主要功能已完成，等待后端接口接入
**下一步**：接入真实数据接口，完善拍摄功能连通